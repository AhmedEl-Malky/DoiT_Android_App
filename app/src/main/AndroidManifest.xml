<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoiTApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.Activities.TypingNoteActivity"
            android:exported="false"
            android:theme="@style/typing_note_activity_style" />
        <activity
            android:name=".ui.Activities.SplashScreen"
            android:exported="true"
            android:theme="@style/splash_screen_style">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Activities.WalkThroughActivity"
            android:exported="false"
            android:theme="@style/walkthrough_style" />
        <activity
            android:name=".ui.Activities.MainActivity"
            android:exported="false"
            android:theme="@style/main_activity_style" />
    </application>

</manifest>